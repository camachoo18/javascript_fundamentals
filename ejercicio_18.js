const frase_original = "Hola mundo";

function cambioVocales(string, vocal){
    let nueva_frase = "hala manda";

    return nueva_frase;
}

const frase_modificada = cambioVocales(frase_original, "a"); //esto deberia devolver "Hala manda"
console.log(frase_modificada)